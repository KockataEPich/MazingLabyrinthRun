#include "../include/world/worldObjectHolder.h"

//WorldObjectHolder::WorldObjectHolder() { }
//
//void WorldObjectHolder::initializeWorld(const sf::Vector2u& window_size) {
//	// populate the world with grass
//	int tiles_x = window_size.x / 32;
//	int tiles_y = window_size.y / 32;
//
//	for(int i = 0; i < window_size.x; i = i + 32)
//		for(int j = 0; j < window_size.y; j = j + 32)
//			add_to_world_objects(*m_tileFactory.create(TileType::grass, sf::Vector2f(i, j)));
//}